<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Eligibility Results</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body class="bg-gray-100">
  <div class="max-w-4xl mx-auto p-6">
    <div class="bg-white rounded-lg shadow-lg p-8">
      <h1 class="text-3xl font-bold text-center mb-8 text-green-600">
        <i class="fas fa-clipboard-list mr-2"></i>Your Eligibility Results
      </h1>

      <% if (results.programs.length > 0) { %>
        <div class="mb-8">
          <h2 class="text-xl font-semibold mb-4 text-green-700">
            <i class="fas fa-check-circle mr-2"></i>You qualify for these programs:
          </h2>
          <div class="space-y-4">
            <% results.programs.forEach(program => { %>
              <div class="p-4 bg-green-50 border border-green-200 rounded-lg">
                <h3 class="font-medium text-green-800">
                  <i class="fas fa-chevron-right mr-2 text-green-600"></i><%= program %>
                </h3>
              </div>
            <% }); %>
          </div>
        </div>
      <% } %>

      <% if (results.ineligibleNotes.length > 0) { %>
        <div class="mb-8">
          <h2 class="text-xl font-semibold mb-4 text-yellow-700">
            <i class="fas fa-info-circle mr-2"></i>Additional Opportunities:
          </h2>
          <div class="space-y-4">
            <% results.ineligibleNotes.forEach(note => { %>
              <div class="p-4 bg-yellow-50 border border-yellow-200 rounded-lg">
                <p class="text-yellow-800">
                  <i class="fas fa-lightbulb mr-2 text-yellow-600"></i><%= note %>
                </p>
              </div>
            <% }); %>
          </div>
        </div>
      <% } %>

      <div class="text-center mt-8">
        <a href="/" class="inline-block bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-6 rounded-lg transition duration-200">
          <i class="fas fa-redo mr-2"></i>Start New Assessment
        </a>
      </div>
    </div>
  </div>
</body>
</html>